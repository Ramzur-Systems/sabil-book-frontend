# Base URL of the Sabil Books REST API.
VITE_API_BASE_URL=/api
# When "true", MSW intercepts API calls in dev with seeded fixtures.
# Set to "false" once the Django marketplace endpoints are live.
VITE_USE_MOCKS=true
